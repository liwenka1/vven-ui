{
	"name": "vven-ui",
	"version": "0.0.1",
	"description": "一个未来可期的 ui 库",
	"author": "李文凯",
	"license": "MIT",
	"scripts": {
		"build": "tsup",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"devDependencies": {
		"tsup": "^8.0.1"
	}
}
